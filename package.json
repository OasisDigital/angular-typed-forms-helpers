{
  "scripts": {
    "clear-dist": "node _tools/clean.js ./package/dist",
    "prep": "cp './README.md' './package/README.md' -f && cp './LICENSE' './package/LICENSE' -f",
    "build": "npm run clear-dist && npm run prep && cd ./package && npm run tsc",
    "publish": "cd ./package && npm publish"
  },
  "dependencies": {
    "@angular/forms": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
